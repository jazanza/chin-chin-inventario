// ... (rest of the file remains the same)

// Fix missing parameters in saveMasterProductConfig
export const saveMasterProductConfig = async (configToSave: MasterProductConfig) => {
  // ... (rest of the function remains the same)
};

// ... (rest of the file remains the same)